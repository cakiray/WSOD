data:
  input_channels: 4
  num_classes: 1
  ignore_label: 255
  training_size: 3712

train:
  seed: 15
  deterministic: False

dataset:
  name: kitti
  root: /data
  data_path: dataset/kitti/object/training/velodyne
  crm_path: Ezgi/center_response_map_r2
  num_points: 20000
  voxel_size: 0.05
  quantization_size: 3
  
num_epochs: 50
prm:
  win_size: 5
  peak_threshold: 0.3

batch_size: 2
batch_norm: false
workers_per_gpu: 2
best_model: /data/Ezgi/best_model
checkpoints: /data/Ezgi/checkpoints
tfevent: /data/Ezgi/runs
tfeventname: /lr0,1-r2
outputs: /data/Ezgi/outputs_r2

model:
  name: spvnas
  no_bn: false

criterion:
  name: mse

optimizer:
  name: adam
  lr: 1.0e-1
  weight_decay: 1.0e-4
  momentum: 0.9
  nesterov: true

scheduler:
  name: cosine_warmup
